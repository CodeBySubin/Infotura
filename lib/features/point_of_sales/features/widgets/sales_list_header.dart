import 'package:flutter/material.dart';

class SalesListHeader extends StatelessWidget {
  const SalesListHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}